package com.kh.MethodEx;

/*
메서드(Method)
	특정 행동이나 작업을 진행하는 코드 블록 { }
	
	Setter : 필드에 작성한 변수를 복사해서 원본 변수 대신에 값을 변경하고 저장하는 메서드
		public void set변수명(자료형 매개변수){
			this.변수 = 매개변수;
		}
	Getter : Setter에 저장된 변수값을 다른곳에서 사용할 수 있도록 전달해주는 메서드
		public 자료형 get변수명() {
			return 변수명;
		}
		
	생성자 : 파일명, 클래스이름과 똑같이 작성을 해줘야하고 객체의 기본 값을 설정하거나 값을 각각 변경해서 사용하는데 쓰이는 메서드
		기본 : 매개변수가 필수로 없어도 되는 생성자 / 필수 입력이 없음
		public 클래스이름(){
			
		}
		필수 : 매개변수가 필수로 존재해야하는 생성자 / 필수로 작성해줬으면 하는 값을 표기
		public 클래스이름(자료형 매개변수,자료형 매개변수){
			this.변수명 = 매개변수;
			this.변수명 = 매개변수;
		}
			예를들어 회원가입을 진행할 때 필수로 이름 나이 () 넣어주고
				결혼기념일 광고수신동의는 선택으로 하고 싶다면 필수로 이름 ~ 주소를 넣어줌
			public 클래스이름 (String name, int age) {
				this.name = name;
				this.age = age;
			}
			
			void : 어떤 행동을 수행하기 위해 작성하고, 수행한 다음 특별히 전달할 내용이 없을 때 사용
			
			return
 * */
public class MethodEx {

}
