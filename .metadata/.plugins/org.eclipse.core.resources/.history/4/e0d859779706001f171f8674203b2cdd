package com.kh.IfEx;


/*
 1. if
 만약에 (  ) 안에 작성한 조건이 true = 참일 때만 특정 코드블록을 실행하는 제어문
 
 if (조건) {
 	조건이 참일 때 실행하는 코드
 }
 
 2. if - else if - else
 
 if와 else는 최초 1회만 작성 가능하고 if는 맨 앞에 작성 else는 맨 끝에 작성
 
 if는 생략이 불가능하지만 else if와 else는 생략이 가능함
 
 else if는 1회만 작성하는 것이 아니라 만들고 싶은 조건 만큼 무한정 만들 수 있음
 
 if (조건1) {
 	조건1이 참일때 실행되는 코드
 } else if(조건2) {
 	조건1이 거짓이고 조건2가 참일 때 실행되는 코드
 } else {
 	위 모든 조건이 거짓일 때 실행되는 코드
 }
 
 3. if - else 
 	if의 조건이 거짓일 때 실행하고자 하는 내용이 있다면 else를 사용해서 { } 안에 거짓에 대한 내용을 작성
 	if(조건) {
 		조건이 참일 때 실행되는 코드들
 	} else {
 		조건이 거짓일 때 실행되는 코드들
 	}
 * */
public class IF문 {

}
